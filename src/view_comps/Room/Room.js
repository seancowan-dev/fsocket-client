import React from 'react';
import { inject, observer } from 'mobx-react';

const Room = observer((props) => {
    return (
        <div className="room">

        </div>
    );
});

export default Room;